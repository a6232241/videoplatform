{
  "name": "bilibili-koa2-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node bin/www",
    "dev": "./node_modules/.bin/nodemon bin/www",
    "prd": "pm2 start bin/www",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@koa/multer": "^3.0.0",
    "koa-session": "^6.0.0",
    "multer": "^1.4.2",
    "mysql": "^2.18.1",
    "nodemon": "^1.12.1"
  }
}
